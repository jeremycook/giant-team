@using System.Text.Json
@{
    var libs = new[]
    {
        "chart.js/auto",
    };
    var importmap = new
    {
        imports = libs.ToDictionary(lib => lib, lib => $"https://cdn.skypack.dev/{lib}")
    };
}
<script type="importmap">@Json.Serialize(importmap)</script>